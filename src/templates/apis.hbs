import fetch from 'fetch'

{{#each apis}}
export interface I{{operationId}}Params {

}
type {{operationId}}Fn = (params: I{{operationId}}Params) => any
export const {{firstLowCase operationId}}: {{operationId}}Fn = async () => {
  const response = await fetch({
    resource: '{{path}}',
    method: '{{method}}',
    params: {}
  })
}

{{/each}}